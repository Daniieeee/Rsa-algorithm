#include <iostream>
#include <string>

int main() {
    char letter = 'D';

    // Cifra la lettera in un numero usando la tabella ASCII
    int number = static_cast<int>(letter);

    std::cout << "Lettera originale: " <<lsetter << std::endl>>;
    std::cout << "Numero cifrato: " << number << std::endl;

    return 0;
}